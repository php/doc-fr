<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.7 $ -->
<!-- EN-Revision: 1.8 Maintainer: didou Status: ready -->
 <refentry id="function.imap-createmailbox">
  <refnamediv>
   <refname>imap_createmailbox</refname>
   <refpurpose>Crée une nouvelle boîte aux lettres</refpurpose>
  </refnamediv>
  <refsect1>
   <title>Description</title>
   <methodsynopsis>
    <type>bool</type><methodname>imap_createmailbox</methodname>
    <methodparam><type>resource</type><parameter>imap_stream</parameter></methodparam>
    <methodparam><type>string</type><parameter>mbox</parameter></methodparam>
   </methodsynopsis>
   <para>
    <function>imap_createmailbox</function> crée une nouvelle boîte
    aux lettres nommée <parameter>mbox</parameter>. Les noms
    contenant des caractères spéciaux doivent être
    encodés.
   </para>
   <para>
    <function>imap_createmailbox</function> retourne &true;
    en cas de succès, et &false; en cas d'erreur.
   </para>
   <para>
    <example>
     <title>Exemple avec <function>imap_createmailbox</function></title>
     <programlisting role="php">
<![CDATA[
<?php

$mbox = imap_open("{your.imap.host}", "utilisateur", "mot_de_passe", OP_HALFOPEN)
    or die("connexion impossible: " . imap_last_error());
    
$name1 = "nouvellephpbox";
$name2 = imap_utf7_encode("nouvellephpboxéx");

$newname = $name1;

echo "Le nouveau nom sera '$name1'<br />\n";

// Nous allons créer maintenant une nouvelle boîte aux lettres "phptestbox"
// dans votre dossier inbox, vérifier son état et finalement, la supprimer
// pour remettre votre inbox dans son état initial.
if (@imap_createmailbox($mbox, imap_utf7_encode("{your.imap.host}INBOX.$newname"))) {
    $status = @imap_status($mbox, "{your.imap.host}INBOX.$newname", SA_ALL);
    if ($status) {
        echo "Votre nouvelle boîte '$name1' est dans l'état suivant :<br />\n";
        echo "Messages : "     . $status->messages    . "<br />\n";
        echo "Récent : "       . $status->recent      . "<br />\n";
        echo "Non lus : "      . $status->unseen      . "<br />\n";
        echo "UID suivant : "  . $status->uidnext     . "<br />\n";
        echo "UID validité : " . $status->uidvalidity . "<br />\n";
   
        if (imap_renamemailbox($mbox, "{your.imap.host}INBOX.$newname", "{your.imap.host}INBOX.$name2")) {
            echo "renommage de la boîte aux lettres '$name1' en '$name2'<br />\n";
            $newname = $name2;
        } else {
            echo "imap_renamemailbox sur la nouvelle boîte aux lettres a échoué : " . imap_last_error() . "<br />\n";
        }
    } else {
        echo "imap_status sur la nouvelle boîte aux lettres a échoué : " . imap_last_error() . "<br />\n";
    }

    if(@imap_deletemailbox($mbox, "{your.imap.host}INBOX.$newname")) {
        echo "new mailbox supprimée pour remettre tout en état<br />\n";
    } else {
        echo "imap_deletemailbox sur la nouvelle boîte aux lettres a échoué : " . implode("<br />\n", imap_errors()) . "<br />\n";
    }

} else {
    echo "Impossible de créer une nouvelle boîte aux lettres :  " . implode("<br />\n", imap_errors()) . "<br />\n";
}
imap_close($mbox);
?>
]]>
     </programlisting>
    </example>
   </para>
   <para>
    Voir aussi
    <function>imap_renamemailbox</function>,
    <function>imap_deletemailbox</function> et
    <function>imap_open</function> pour connaître le format des noms de
    <parameter>mbox</parameter>.
   </para>
  </refsect1>
 </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
