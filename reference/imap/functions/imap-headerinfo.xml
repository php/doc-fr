<?xml version="1.0" encoding="iso-8859-1"?>
<!-- splitted from ./fr/functions/imap.xml, last change in rev 1.15 -->
<!-- last change to 'imap-headerinfo' in en/ tree in rev 1.33 -->
 <refentry id="function.imap-headerinfo">
  <refnamediv>
   <refname>imap_headerinfo</refname>
   <refpurpose>Lit l'en-tête  du message</refpurpose>
  </refnamediv>
  <refsect1>
   <title>Description</title>
    <methodsynopsis>
     <type>object</type><methodname>imap_headerinfo</methodname>
     <methodparam><type>resource</type><parameter>imap_stream</parameter></methodparam>
     <methodparam><type>int</type><parameter>msg_number</parameter></methodparam>
     <methodparam choice="opt"><type>int</type><parameter>fromlength</parameter></methodparam>
     <methodparam choice="opt"><type>int</type><parameter>subjectlength</parameter></methodparam>
     <methodparam choice="opt"><type>string</type><parameter>defaulthost</parameter></methodparam>
    </methodsynopsis>
   <para>
    <function>imap_headerinfo</function> retourne un objet contenant
    divers éléments d'en-tête.
   </para>
   <para>
    <informalexample>
     <literallayout>
      remail, date, Date, subject, Subject, in_reply_to, message_id,
      newsgroups, followup_to, references
éléments d'en-tête :
  Recent -  'R' si récent et lu
            'N' si récent et non lu
            ' ' si non récent
  Unseen -  'U' si non lu ET non récent
            ' ' si lu OU non lu et récent
  Answered -'A' si répondu,
            ' ' si non répondu
  Deleted - 'D' si effacé,
            ' ' si non effacé
  Draft -   'X' si brouillon,
            ' ' si non brouillon
  Flagged - 'F' si marqué,
            ' ' si non marqué
Notez bien que le comportement récent/non lu est un peu particulier :
si vous voulez savor si un message est non lu, vous devez le vérifier
avec
Unseen == 'U' || Recent == 'N'
toaddress (toute la ligne d'en-tête To: jusqu'à 1024 caractères)
to[] (retourne un objet avec tout l'en-tête To, contenant):
  personal
  adl
  mailbox
  host
fromaddress (toute la ligne d'en-tête from: jusqu'à 1024 caractères)
from[] (retourne un objet avec tout l'en-tête From, contenant):
  personal
  adl
  mailbox
  host
ccaddress (toute la ligne d'en-tête CC: jusqu'à 1024 caractères)
cc[] (retourne un objet avec tout l'en-tête CC, contenant):
  personal
  adl
  mailbox
  host
bccaddress (toute la ligne d'en-tête BCC: jusqu'à 1024 caractères)
bcc[] (retourne un objet avec tout l'en-tête BCC, contenant):
  personal
  adl
  mailbox
  host
reply_toaddress (oute la ligne d'en-tête Reply_to: jusqu'à 1024 caractères)
reply_to[] (retourne un objet avec tout l'en-tête Reply_to, contenant)
  personal
  adl
  mailbox
  host
senderaddress (toute la ligne d'en-tête Sender: jusqu'à 1024 caractères)
sender[] (retourne un objet avec tout l'en-tête Sender, contenant)
  personal
  adl
  mailbox
  host
return_path (toute la ligne d'en-tête Return-path: jusqu'à 1024 caractères)
return_path[] (retourne un objet avec tout l'en-tête Return-path, contenant)
  personal
  adl
  mailbox
  host
udate (Date du mail, au format UNIX)
fetchfrom (Ligne d'en-tête from formatée pour tenir dans <parameter>fromlength</parameter>
caractères)
fetchsubject (Ligne d'en-tête subject formatée pour tenir dans <parameter>subjectlength</parameter> caractères)
     </literallayout>
    </informalexample>
   </para>
  </refsect1>
 </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
