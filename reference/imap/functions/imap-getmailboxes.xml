<?xml version="1.0" encoding="iso-8859-1"?>
<!-- splitted from ./fr/functions/imap.xml, last change in rev 1.15 -->
<!-- last change to 'imap-getmailboxes' in en/ tree in rev 1.2 -->
 <refentry id="function.imap-getmailboxes">
  <refnamediv>
   <refname>imap_getmailboxes</refname>
   <refpurpose>
    Liste les boîtes aux lettres, et retourne le détail pour chacune.
   </refpurpose>
  </refnamediv>
  <refsect1>
   <title>Description</title>
     <methodsynopsis>
      <type>array</type><methodname>imap_getmailboxes</methodname>
      <methodparam><type>resource</type><parameter>imap_stream</parameter></methodparam>
      <methodparam><type>string</type><parameter>ref</parameter></methodparam>
      <methodparam><type>string</type><parameter>pattern</parameter></methodparam>
     </methodsynopsis>
   <para>
    <function>imap_getmailboxes</function> retourne un tableau d'objets contenant
    les informations sur les boîtes aux lettres. Chaque objet a les attributs
    de <parameter>name</parameter>, qui contient le nom complet de la boîte
    aux lettres; <parameter>delimiter</parameter>, qui est le délimiteur
    hiérarchique; et <parameter>attributes</parameter>.
    <parameter>attributes</parameter> est un masque de bits, qui contient :
    <itemizedlist>
     <listitem>
      <simpara>
       LATT_NOINFERIORS - Cette boîte aux lettres n'a pas d'"enfants"
       (il n'y a plus de boîtes aux lettres en dessous de celle-ci).
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       LATT_NOSELECT - Ceci est juste un container, pas une boîte aux lettres
       (vous ne pouvez pas l'ouvrir).
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       LATT_MARKED - Cette boîte aux lettres est marquée.
       Utilisé uniquement avec UW-IMAPD.
      </simpara>
     </listitem>
     <listitem>
      <simpara>
       LATT_UNMARKED - Cette boîte aux lettres n'est pas
       marquée. Utilisé uniquement avec UW-IMAPD.
      </simpara>
     </listitem>
    </itemizedlist>
   </para>
   <para>
    <parameter>ref</parameter> ne devrait être que le serveur <acronym>IMAP</acronym>
    sous la forme <literal>{imap_server:imap_port}</literal>, et
    <parameter>pattern</parameter> spécifie la position dans la
    hiérarchie des boîtes aux lettres, où il faut
    commencer à charcher. Si vous voulez passer en revue toute la
    hiérarchier, passez '*' comme <parameter>pattern</parameter>.
   </para>
   <para>
    Il y a deux caractères spéciaux que vous pouvez utiliser dans
    <parameter>pattern</parameter> : '*' et  '%'. '*' signifie : toutes les
    boîtes aux lettres. Si vous passez <parameter>pattern</parameter>
    comme '*', vous obtiendrez la liste complète des boîtes aux
    lettres de la hiérarchie. '%' signifie qu'on ne s'interesse qu'au
    niveau courant. '%' passé à <parameter>pattern</parameter>
    ne retournera que les boîtes aux lettres de niveau supérieur;
    '~/mail/%'.Sous UW_IMAPD retournera toutes les boîtes aux lettres du
    dossier  <filename>~/mail directory</filename>, mais pas leurs enfants.
   </para>
   <para>
    <example>
     <title>Exemple avec <function>imap_getmailboxes</function></title>
     <programlisting role="php">
<![CDATA[
<?php
$mbox = imap_open("{your.imap.host}","utilisateur","mot_de_passe",OP_HALFOPEN)
     or die("connexion impossible : ".imap_last_error());
$list = imap_getmailboxes($mbox,"{votre.hote.imap}","*");
if(is_array($list)) {
  reset($list);
  while (list($key, $val) = each($list)) {
   print "($key) ";
   print imap_utf7_decode($val->name).",";
   print "'".$val->delimiter."',";
   print $val->attributes."<BR>\n";
  }
} else {
  print "imap_getmailboxes a échoué : ".imap_last_error()."\n";
}
imap_close($mbox);
?>
]]>
     </programlisting>
    </example>
   </para>
   <para>
    Voir aussi
    <function>imap_getsubscribed</function>.
   </para>
  </refsect1>
 </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
