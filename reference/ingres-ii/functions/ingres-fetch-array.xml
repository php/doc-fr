<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.10 $ -->
<!-- EN-Revision: 1.6 Maintainer: yannick Status: ready -->
<!-- Reviewed: yes -->
 <refentry id="function.ingres-fetch-array">
  <refnamediv>
   <refname>ingres_fetch_array</refname>
   <refpurpose>
    Récupère une ligne de résultat Ingres dans un tableau
   </refpurpose>
  </refnamediv>
  <refsect1>
   &reftitle.description;
    <methodsynopsis>
     <type>array</type><methodname>ingres_fetch_array</methodname>
     <methodparam choice="opt"><type>int</type><parameter>result_type</parameter></methodparam>
     <methodparam choice="opt"><type>resource</type><parameter>link</parameter></methodparam>
    </methodsynopsis>
   <para>
    <function>ingres_fetch_array</function> renvoie un tableau correspondant
    à la ligne récupérée, ou &false;
    s'il n'y a plus de ligne à récupérer.
   </para>
   <para>
    Cette fonction est une version améliorée de
    <function>ingres_fetch_row</function>. En plus de stocker les données
    dans un tableau à indices numériques, elle peut aussi les enregistrer
    dans un tableau associatif, en utilisant les noms des champs comme
    indices.
   </para>
   <para>
    Si plusieurs colonnes ont le même nom, la dernière colonne aura la
    priorité. Pour accéder aux autres colonnes du même nom, vous devez
    utiliser l'index numérique, ou faire un alias pour chaque colonne.
    <example>
     <title>Exemple avec <function>ingres_fetch_array</function></title>
     <programlisting role="php">
<![CDATA[
<?php

ingres_query("select t1.f1 as foo t2.f1 as bar from t1, t2");
$result = ingres_fetch_array();
$foo = $result["foo"];
$bar = $result["bar"];

?>
]]>
     </programlisting>
    </example>
   </para>
   <para>
    <parameter>result_type</parameter> peut valoir INGRES_NUM pour un
    tableau à indices numériques, INGRES_ASSOC pour un tableau associatif,
    ou INGRES_BOTH (défaut) pour un tableau mixte (accessible selon les deux
    méthodes).
   </para>
   <para>
    Du point de vue de la rapidité, cette fonction est identique à
    <function>ingres_fetch_object</function>, et presque aussi rapide
    que <function>ingres_fetch_row</function> (la différence est
    insignifiante).
   </para>
   <para>
    <example>
     <title>Exemple pour <function>ingres_fetch_array</function></title>
     <programlisting role="php">
<![CDATA[
<?php
ingres_connect($database, $user, $password);

ingres_query("select * from table");
while ($row = ingres_fetch_array()) {
    echo $row["user_id"]; // utilisation du tableau associatif
    echo $row["fullname"];
    echo $row[1];         // utilisation du tableau à indices numériques
    echo $row[2];
}
?>
]]>
     </programlisting>
    </example>
   </para>
   <para>
    Voir aussi
    <function>ingres_query</function>,
    <function>ingres_num_fields</function>,
    <function>ingres_field_name</function>,
    <function>ingres_fetch_object</function> et
    <function>ingres_fetch_row</function>.
   </para>
  </refsect1>
 </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
