<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
<!-- EN-Revision: fa1ad00f0059f3a33252a6921079eb40319f16e2 Maintainer: yannick Status: ready -->
<!-- Reviewed: no -->

<refentry xml:id="amqpqueue.ack" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 <refnamediv>
  <refname>AMQPQueue::ack</refname>
  <refpurpose>Accuse réception d'un message</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <modifier>public</modifier> <type>bool</type><methodname>AMQPQueue::ack</methodname>
   <methodparam><type>int</type><parameter>delivery_tag</parameter></methodparam>
   <methodparam choice="opt"><type>int</type><parameter>flags</parameter><initializer>NULL</initializer></methodparam>
  </methodsynopsis>
  <para>
   Cette méthode autorise un accusé de réception d'un message
   qui a été reçu avec le drapeau <constant>AMQP_NOACK</constant>
   via la méthode <methodname>AMQPQueue::get</methodname> ou
   la méthode <methodname>AMQPQueue::consume</methodname>.
  </para>
 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  <para>
   <variablelist>
    <varlistentry>
     <term><parameter>delivery_tag</parameter></term>
     <listitem>
      <para>
       Le drapeau du message délivré.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term><parameter>flags</parameter></term>
     <listitem>
      <para>
       Le seul drapeau valide qui peut être passé est
       <constant>AMQP_MULTIPLE</constant>.
      </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </para>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>
   &return.success;
  </para>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <para>
   <example>
    <title><methodname>AMQPQueue::ack</methodname> example with <methodname>AMQPQueue::get</methodname></title>
    <programlisting role="php">
<![CDATA[
<?php

/* Create a connection using all default credentials: */
$connection = new AMQPConnection();
$connection->connect();

/* create a queue object */
$queue = new AMQPQueue($connection);

//declare the queue
$queue->declare('myqueue');

//get the next message, but don't mark it as delivered
$message = $queue->get(AMQP_NOACK);

echo $message['msg'];

//acknowledge the message as received
$queue->ack($message['delivery_tag']);

?>
]]>
    </programlisting>
   </example>
   <example>
    <title>Exemple avec <methodname>AMQPQueue::ack</methodname> et <methodname>AMQPQueue::consume</methodname></title>
    <programlisting role="php">
<![CDATA[
<?php

/* Crée une connexion en utilisant les identifiants de connexion par défaut : */
$connection = new AMQPConnection();
$connection->connect();

/* Création d'un objet de file d'attente */
$queue = new AMQPQueue($connection);

// déclaration de la file d'attente
$queue->declare('myqueue');

$options = array(
    'min' => 1,
    'max' => 10,
    'ack' => false
);

// récupère les messages, sans les marquer comme délivrés
$messages = $queue->consume($options);

foreach ($messages as $message) {
    echo $message['message_body'];
    // accusé de réception du message, tel que reçu
    $queue->ack($message['delivery_tag']);
}

?>
]]>
    </programlisting>
   </example>
  </para>
 </refsect1>
 
</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
