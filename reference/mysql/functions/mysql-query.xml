<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.12 $ -->
<!-- EN-Revision: 1.14 Maintainer: dams Status: ready -->
<!-- Reviewed: no -->
  <refentry id="function.mysql-query">
   <refnamediv>
    <refname>mysql_query</refname>
    <refpurpose>Envoi une requête à un serveur MySQL</refpurpose>
   </refnamediv>

   <refsect1 role="description">
    &reftitle.description;
    <methodsynopsis>
     <type>resource</type><methodname>mysql_query</methodname>
     <methodparam><type>string</type><parameter>query</parameter></methodparam>
     <methodparam choice="opt"><type>resource</type><parameter>link_identifier</parameter></methodparam>
    </methodsynopsis>
    <para>
     <function>mysql_query</function> envoie une requête (au serveur MySQL
     actuellement actif sur le serveur associé avec le 
     <parameter>link_identifier</parameter> spécifié).
    </para>
   </refsect1>

   <refsect1 role="parameters">
    &reftitle.parameters;
    <para>
     <variablelist>
      <varlistentry>
       <term><parameter>query</parameter></term>
       <listitem>
        <para>
         Une requête SQL
        </para>
        <para>
         La chaîne de requête ne doit pas se terminer par un point-virgule.
        </para>
       </listitem>
      </varlistentry>
      <varlistentry>
       <term><parameter>link_identifier</parameter></term>
       <listitem>
        <para>
         Une connexion retournée par la fonction 
         <function>mysql_connect</function>.
        </para>
        <para>
         Si <parameter>link_identifier</parameter> n'est pas spécifié, la dernière
         connexion ouverte sera utilisée. Si aucune connexion n'est ouverte, la fonction 
         tente d'en établir une en appelant la fonction <function>mysql_connect</function> 
         avec aucun argument et l'utilise. Le résultat de la requête est mis en cache.
        </para>
       </listitem>
      </varlistentry>
     </variablelist>
    </para>
   </refsect1>

   <refsect1 role="returnvalues">
    &reftitle.returnvalues;
    <para>
     Pour les requêtes du type <literal>SELECT</literal>, <literal>SHOW</literal>,
     <literal>DESCRIBE</literal> ou <literal>EXPLAIN</literal>,
     <function>mysql_query</function> retournera une &resource; en cas de succès,
     et &false; en cas d'erreur.
    </para>
    <para>
     Pour les autres types de requêtes, <literal>UPDATE</literal>, 
     <literal>DELETE</literal>, <literal>DROP</literal>, etc., 
     <function>mysql_query</function> retourne &true; en cas de succès
     et &false; en cas d'erreur.
    </para>
    <para>
     La ressource de résultat retournée doit être passée à la fonction
     <function>mysql_fetch_array</function>, et les autres fonctions
     permettant d'explorer le résultat des tables, pour accéder aux 
     données retournées.
    </para>
    <para>
     Utilisez <function>mysql_num_rows</function> pour trouver le nombre
     de lignes retournées pour une requête du type <literal>SELECT</literal> ou
     <function>mysql_affected_rows</function> pour trouver le nombre
     de lignes affectées par les requpetes du type <literal>DELETE</literal>, 
     <literal>INSERT</literal>, <literal>REPLACE</literal>, ou <literal>UPDATE</literal>.
    </para>
    <para>
     <function>mysql_query</function> échouera et retournera &false;
     si l'utilisateur n'a pas les autorisations nécessaire pour accéder à la (aux) table(s)
     référencée(s) par la requête.
    </para>
   </refsect1>

   <refsect1 role="examples">
    &reftitle.examples;
    <para>
     <example>
      <title>Requête invalide</title>
      <para>
       La requête suivante est syntaxiquement invalide, donc 
       <function>mysql_query</function> va échouer et retourner &false;.
      </para>
      <programlisting role="php">
<![CDATA[
<?php
$result = mysql_query('SELECT * WHERE 1=1');
if (!$result) {
    die('Requête invalide : ' . mysql_error());
}

?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     <example>
      <title>Requête valide</title>
      <para>
       La requête suivante est valide, donc <function>mysql_query</function>
       va retourner une &resource;.
      </para>
      <programlisting role="php">
<![CDATA[
<?php
// Ceci peut être demandé par un utilisateur, par exemple :
$firstname = 'fred';
$lastname  = 'fox';

// Formulation de la requête
// C'est la meilleur façon d'exécuter une requête SQL
// Pour plus d'exemples, voir mysql_real_escape_string()
$query = sprintf("SELECT firstname, lastname, address, age FROM friends WHERE firstname='%s' AND lastname='%s'",
    mysql_real_escape_string($firstname),
    mysql_real_escape_string($lastname));

// Exécution de la requête
$result = mysql_query($query);

// Vérification du résultat
// Ceci montre la requête envoyée à MySQL ainsi que l'erreur. Utile pour déboguer.
if (!$result) {
    $message  = 'Requête invalide : ' . mysql_error() . "\n";
    $message .= 'Requête complète : ' . $query;
    die($message);
}

// Utilisation du résultat
// Tenter d'affichager $result ne vous donnera pas d'informations contenues dans la ressource
// Une des fonctions MySQL de résultat doit être utilisée
// Voir aussi mysql_result(), mysql_fetch_array(), mysql_fetch_row(), etc.
while ($row = mysql_fetch_assoc($result)) {
    echo $row['firstname'];
    echo $row['lastname'];
    echo $row['address'];
    echo $row['age'];
}

// Libération des ressources associées au jeu de résultats
// Ceci est effectué automatiquement à la fin du script
mysql_free_result($result);
?>
]]>
      </programlisting>
     </example>
    </para>
   </refsect1>

   <refsect1 role="seealso">
    &reftitle.seealso;
    <para>
     <simplelist>
      <member><function>mysql_connect</function></member>
      <member><function>mysql_error</function></member>
      <member><function>mysql_real_escape_string</function></member>
      <member><function>mysql_result</function></member>
      <member><function>mysql_fetch_assoc</function></member>
      <member><function>mysql_unbuffered_query</function></member>
     </simplelist>
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
