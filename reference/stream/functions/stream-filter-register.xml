<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.3 $ -->
  <refentry id="function.stream-filter-register">
   <refnamediv>
    <refname>stream_filter_register</refname>
    <refpurpose>Enregistre un filtre de flux</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Description</title>
    <methodsynopsis>
     <type>bool</type><methodname>stream_filter_register</methodname>
     <methodparam><type>string</type><parameter>filtername</parameter></methodparam>
     <methodparam><type>string</type><parameter>classname</parameter></methodparam>
    </methodsynopsis>
    <para>
     <function>stream_filter_register</function> vous permet d'impl&eacute;menter
     votre propre filtre de flux, &agrave; utiliser avec les fonctions d'acc&egrave;s
     aux donn&eacute;es externes (comme <function>fopen</function>,
     <function>fread</function>, etc...).
    </para>
    <para>
     Pour ce faire, vous devez d&eacute;finir une classe qui &eacute;tend la classe
     <literal>php_user_fitler</literal> avec les membres et m&eacute;thodes d&eacute;finies
     ci-dessous. Lorsque vous r&eacute;alisez des op&eacute;rations de lecture et d'&eacute;criture
     dans le flux auquel votre filtre est attach&eacute;, &php; passera les donn&eacute;es
     &agrave; travers votre filtre (et tous les autres filtres attach&eacute;s), de fa&ccedil;on
     &agrave; ce que les donn&eacute;es soient modifi&eacute;es telles que d&eacute;sir&eacute;. Vous devez
     impl&eacute;menter les m&eacute;thodes telles que d&eacute;crit ci-dessous, sous peine de
     comportement ind&eacute;fini.
    </para>
    <para>
     <function>stream_filter_register</function> retourne &false; si le nom
     de filtre <parameter>filtername</parameter> est d&eacute;j&agrave; utilis&eacute;.
    </para>

    <methodsynopsis>
     <type>int</type><methodname>filter</methodname>
     <methodparam><type>resource</type><parameter>in</parameter></methodparam>
     <methodparam><type>resource</type><parameter>out</parameter></methodparam>
     <methodparam><type>int</type><parameter>&amp;consumed</parameter></methodparam>
     <methodparam><type>bool</type><parameter>closing</parameter></methodparam>
    </methodsynopsis>
    <para>
     Cette m&eacute;thode est appel&eacute; &agrave; chaque fois que des donn&eacute;es sont lues ou &eacute;crites
     dans le flux attach&eacute; (avec des fonctions comme <function>fread</function> ou
     <function>fwrite</function>).  Le param&egrave;tre <parameter>in</parameter> est une ressource 
     qui pointe sur une <literal>bucket brigade</literal> qui contient un ou plusieurs
     objet <literal>bucket</literal> contenant les donn&eacute;es &agrave; filtrer.
     <parameter>out</parameter> est une autre ressource 
     qui pointe sur une <literal>bucket brigade</literal>
     dans laquelle les donn&eacute;e seront plac&eacute;es.
     <parameter>consumed</parameter>, qui doit <emphasis>toujours</emphasis>
     &ecirc;tre d&eacute;clar&eacute; par r&eacute;f&eacute;rence, doit &ecirc;tre incr&eacute;ment&eacute; de la taille de donn&eacute;es que
     votre filtre lit et modifie. Dans la plupart des cas, cela signifie que vous
     devrez incr&eacute;menter <parameter>consumed</parameter> avec <literal>$bucket->datalen</literal>
     pour chaque <literal>$bucket</literal>.
     Si le flux est en cours de fermeture (et par cons&eacute;quent, cela sera le dernier
     passage dans la cha&icirc;ne de filtres), le param&egrave;tre <parameter>closing</parameter> 
     vaudra &true; La m&eacute;thode <methodname>filter</methodname> doit retourner l'une 
     des trois valeurs suivantes : <constant>PSFS_PASS_ON</constant> indique la r&eacute;ussite,
     et les donn&eacute;es sont dans le param&egrave;tre <parameter>out</parameter>, une <literal>bucket brigade</literal>.
     <constant>PSFS_FEED_ME</constant> indique que le filtre n'a aucune donn&eacute;es &agrave; retourner, et
     requiert d'autres donn&eacute;es du flux.
     <constant>PSFS_ERR_FATAL</constant> indique que le flux a rencontr&eacute; une erreur
     fatale, et ne peut continuer. Si aucune valeur n'est retourn&eacute;e par cette m&eacute;thode,
     <constant>PSFS_ERR_FATAL</constant> est utilis&eacute;.
   </para>
    
    <methodsynopsis>
     <type>void</type><methodname>oncreate</methodname>
     <void/>
    </methodsynopsis>
    <para>
     Cette m&eacute;thode est appel&eacute;e durant l'instantiation du filtre. Si votre 
     filtre alloue ou initialise d'autres ressources (comme des
     buffers), c'est le moment de le faire.
    </para>

    <methodsynopsis>
     <type>void</type><methodname>onclose</methodname>
     <void/>
    </methodsynopsis>
    <para>
     Cette m&eacute;thode est appel&eacute;e durant l'extinction du filtre (g&eacute;n&eacute;ralement,
     lorsque le flux est ferm&eacute;), et est ex&eacute;cut&eacute; <emphasis>apr&egrave;s</emphasis>
     l'appel de la fonction <literal>flush</literal>.  Si aucune ressource
     a &eacute;t&eacute; allou&eacute;e ou cr&eacute;&eacute;e durant <literal>oncreate()</literal>,
     c'est le moment de les lib&eacute;rer.
    </para>

    <para>
     L'exemple ci-dessous impl&eacute;mente un filtre appel&eacute;
     <literal>rot13</literal>, sur le flux <literal>foo-bar.txt</literal>,
     qui r&eacute;alise un chiffrement de type ROT-13 sur toutes les lettres 
     lue ou &eacute;crite dans le flux.
     <example> 
      <title>Filtre ROT13 pour les flux</title>
      <programlisting role="php">
<![CDATA[
<?php

/* Definition de notre classe de filtre */
class rot13_filter extends php_user_filter {
  function read($length) {
    $tempstr = parent::read($length);
    for($i = 0; $i < strlen($tempstr); $i++)
      if (($tempstr[$i] >= 'A' AND $tempstr[$i] <= 'M') OR
          ($tempstr[$i] >= 'a' AND $tempstr[$i] <= 'm')) $tempstr[$i] = chr(ord($tempstr[$i]) + 13);
      else if (($tempstr[$i] >= 'N' AND $tempstr[$i] <= 'Z') OR
               ($tempstr[$i] >= 'n' AND $tempstr[$i] <= 'z')) $tempstr[$i] = chr(ord($tempstr[$i]) - 13);
    return $tempstr;
  }

  function write($data) {
    for($i = 0; $i < strlen($data); $i++)
      if (($data[$i] >= 'A' AND $data[$i] <= 'M') OR
          ($data[$i] >= 'a' AND $data[$i] <= 'm')) $data[$i] = chr(ord($data[$i]) + 13);
      else if (($data[$i] >= 'N' AND $data[$i] <= 'Z') OR
               ($data[$i] >= 'n' AND $data[$i] <= 'z')) $data[$i] = chr(ord($data[$i]) - 13);
    return parent::write($data);
  }
}

/* Enregistrement du filtre avec PHP */
stream_filter_register("rot13", "rot13_filter")
    or die("Impossible d'enregistrer le filtre");

$fp = fopen("foo-bar.txt", "w");

/* Liaison du filtre avec le flux */
stream_filter_append($fp, "rot13");

fwrite($fp, "Line1\n");
fwrite($fp, "Word - 2\n");
fwrite($fp, "Easy As 123\n");

fclose($fp);

/* Le filtre ne s'applique qu'au flux $fp
 * ce qui fait que cette lecture se fait sans d&eacute;chiffrement
 */
readfile("foo-bar.txt");

/* affichage
 * ------

Yvar1
Jbeq - 2
Rnfl Nf 123

 */
?>
]]>
      </programlisting>
     </example>
    </para>
    <simpara>
     Voir aussi
     <function>stream_register_wrapper</function>,
     <function>stream_filter_prepend</function> et
     <function>stream_filter_append</function>
    </simpara>
   </refsect1>
  </refentry>


<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
