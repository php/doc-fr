<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.3 $ -->
<!-- splitted from ./en/functions/pgsql.xml, last change in rev 1.2 -->
  <refentry id="function.pg-send-query">
   <refnamediv>
    <refname>pg_send_query</refname>
    <refpurpose>Ex&eacute;cute une requ&ecirc;te PostGreSQL asynchrone</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Description</title>
     <methodsynopsis>
      <type>bool</type><methodname>pg_send_query</methodname>
      <methodparam><type>resource</type><parameter>connection</parameter></methodparam>
      <methodparam><type>string</type><parameter>query</parameter></methodparam>
     </methodsynopsis>
     <methodsynopsis>
      <type>bool</type><methodname>pg_send_query</methodname>
      <methodparam><type>string</type><parameter>query</parameter></methodparam>
     </methodsynopsis>
    <para>
     <function>pg_send_query</function> ex&eacute;cute la requ&ecirc;te <parameter>query</parameter>
     sur la connexion <parameter>connection</parameter>, de mani&egrave;re asynchrone.
     Contrairement &agrave; <function>pg_query</function>, cette fonction permet d'envoyer
     plusieurs requ&ecirc;tes &agrave; un serveur PostgreSQL et de lire les r&eacute;sultats un par
     un, avec la fonction <function>pg_get_result</function>. L'ex&eacute;cution du script n'est
     pas bloqu&eacute;e durant l'ex&eacute;cution de la requ&ecirc;te. Utilisez 
     <function>pg_connection_busy</function> pour v&eacute;rifier si la connexion est
     encore occup&eacute;e. Les requ&ecirc;tes peuvent &ecirc;tre annul&eacute;es avec 
     <function>pg_cancel_query</function>.
    </para>
    <para>
     Bien que l'on puiss envoyer plusieurs requ&ecirc;tes en m&ecirc;me temps,
     il n'est pas possible d'envoyer plusieurs requ&ecirc;tes sur une connexion
     occup&eacute;e. Si une requ&ecirc;te est envoy&eacute;e alors que la connexion est occup&eacute;e,
     elle attendra que la requ&ecirc;te pr&eacute;c&eacute;dente soit finie.
    </para>
    <para>
     <example>
      <title>ExŽcute une requte PostGreSQL asynchrone</title>
      <programlisting role="php">
<![CDATA[
<?php
    $dbconn = pg_connect("dbname=publisher") or die("Impossible de se connecter");

    if (!pg_connection_busy($dbconn)) {
        pg_send_query($dbconn,"select * from authors; select count(*) from authors;");
    }
    
    $res1 = pg_get_result($dbconn);
    echo "Premier appel ˆ pg_get_result() : $res1\n";
    $rows1 = pg_num_rows($res1);
    echo "$res1 has $rows1 records\n\n";
    
    $res2 = pg_get_result($dbconn);
    echo "Second appel ˆ pg_get_result() : $res2\n";
    $rows2 = pg_num_rows($res2);
    echo "$res2 has $rows2 records\n";
?>
]]>
      </programlisting>
      <para>
       L'exemple ci-dessus pourrait afficher : 
      </para>
      <screen>
<![CDATA[
Premier appel ˆ pg_get_result() : Resource id #3
Resource id #3 has 3 records

Second appel ˆ pg_get_result() : Resource id #4
Resource id #4 has 1 records
]]>
      </screen>
     </example> 
    </para>
    <para>
     Voir aussi
     <function>pg_query</function>,
     <function>pg_cancel_query</function>,
     <function>pg_get_result</function> et
     <function>pg_connection_busy</function>.
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
