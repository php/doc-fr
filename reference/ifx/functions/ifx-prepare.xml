<?xml version="1.0" encoding="iso-8859-1"?>
<!-- splitted from ./fr/functions/ifx.xml, last change in rev 1.9 -->
<!-- last change to 'ifx-prepare' in en/ tree in rev 1.2 -->
 <refentry id="function.ifx-prepare">
  <refnamediv>
   <refname>ifx_prepare</refname>
   <refpurpose>Pr&eacute;pare une requ&ecirc;te SQL pour l'ex&eacute;cution.</refpurpose>
  </refnamediv>
  <refsect1>
   <title>Description</title>
    <methodsynopsis>
     <type>int</type><methodname>ifx_prepare</methodname>
     <methodparam><type>string</type><parameter>query</parameter></methodparam>
     <methodparam><type>int</type><parameter>conn_id</parameter></methodparam>
     <methodparam choice="opt"><type>int</type><parameter>cursor_def</parameter></methodparam>
     <methodparam><type>mixed</type><parameter>blobidarray</parameter></methodparam>
    </methodsynopsis>
   <para>
    <function>ifx_prepare</function> retourne un entier identifiant de
    r&eacute;sultat <parameter>result_id</parameter> &agrave; utiliser
    avec <function>ifx_do</function>. Modifie la valeur de
    <parameter>affected_rows</parameter>, pour acc&egrave;s ult&eacute;rieur
    avec <function>ifx_affected_rows</function>.
   </para>
   <para>
    <function>ifx_prepare</function> pr&eacute;pare la requ&ecirc;te
    <parameter>query</parameter> sur la connexion <parameter>conn_id</parameter>.
    Pour les requ&ecirc;tes de type "select-type" un pointeur de r&eacute;sultat
    est d&eacute;clar&eacute; et ouvert. L'option <parameter>cursor_type</parameter>
    permet de choisir le type de pointeur : "scroll" et/ou "hold". Les valeurs
    peuvent &ecirc;tre combin&eacute;es ensemble (IFX_SCROLL, IFX_HOLD).
   </para>
   <para>
    Le nombre de ligne affect&eacute;es (estim&eacute; ou exact) est
    enregistr&eacute;, pour &ecirc;tre lu avec la fonction
    <function>ifx_affected_rows</function>.
   </para>
   <para>
    Si vous avez une colonne de type BLOB (BYTE ou TEXT) dans une requ&ecirc;te de
    modification, vous pouvez passer un param&egrave;tre
    <parameter>BLOBidarray</parameter> qui contiendra les identifiants
    des BLOB &agrave; modifier, et vous devrez remplacer cette colonne par
    un point d'interrogation (?) dans la requ&ecirc;te.
   </para>
   <para>
    Si le contenu d'une colonne de type TEXT (ou BYTE) vous pouvez aussi
    utiliser les fonctions <function>ifx_textasvarchar</function> et
    <function>ifx_byteasvarchar</function>. Cela vous permettra d'utiliser
    les colonnes TEXT (ou BYTE) comme des colonnes de type VARCHAR (mais plus
    long, tout de m&ecirc;me), et vous n'aurez pas besoin de l'identifiant de BLOB.
   </para>
   <para>
    Avec les fonctions <function>ifx_textasvarchar</function> et
    <function>ifx_byteasvarchar</function> (valeurs par d&eacute;faut), les
    requ&ecirc;tes SELECT retourneront des identifiants de BLOB.
    Cet identifiant peut &ecirc;tre une cha&icirc;ne ou un fichier,
    suivant la configuration (voir plus loin).
   </para>
   <para>
    Voir aussi
    <function>ifx_do</function>.
   </para>
  </refsect1>
 </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
