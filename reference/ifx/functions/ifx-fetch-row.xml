<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.6 $ -->
<!-- EN-Revision: 1.1 Maintainer: nobody Status: partial -->
<!-- Reviewed: yes -->
 <refentry id="function.ifx-fetch-row">
  <refnamediv>
   <refname>ifx_fetch_row</refname>
   <refpurpose>
    Retourne une ligne sous la forme d'un tableau énuméré.
   </refpurpose>
  </refnamediv>
  <refsect1>
   <title>Description</title>
    <methodsynopsis>
     <type>array</type><methodname>ifx_fetch_row</methodname>
     <methodparam><type>int</type><parameter>result_id</parameter></methodparam>
     <methodparam choice="opt"><type>mixed</type><parameter>position</parameter></methodparam>
    </methodsynopsis>
   <para>
    <function>ifx_fetch_row</function> retourne un tableau associatif qui
    contient la ligne retournée ou &false; s'il ne reste plus de
    lignes à lire, ou s'il a eu une erreur.
   </para>
   <para>
    Les colonnes de types BLOB sont retournées sous la forme d'un
    identifiant à utiliser avec <function>ifx_get_blob</function>
    à moins que vous n'ayez utilisé la fonction
    <function>ifx_textasvarchar</function> ou
    <function>ifx_byteasvarchar</function> et, dans ce cas, les BLOBs
    seront retournés sous forme de chaîne.
    <function>ifx_fetch_row</function> retourne &false;
    en cas d'erreur.
   </para>
   <para>
    <parameter>result_id</parameter> est un identifiant valide de résultat,
    retourné par <function>ifx_query</function> ou
    <function>ifx_prepare</function> (Requêtes SELECT seulement !).
   </para>
   <para>
    <parameter>position</parameter> est un paramètre
    optionnel, pour une opération de lecture d'informations sur un pointeur
    de type "scroll": "NEXT", "PREVIOUS", "CURRENT", "FIRST", "LAST" ou encore un
    nombre. Si vous spécifiez un nombre, la ligne d'index absolu sera
    retournée. Ce paramètre est optionnel, et ne fonctionne qu'avec
    les pointeurs de type "scroll".
   </para>
   <para>
    <function>ifx_fetch_row</function> retourne une ligne de données d'un
    résultat associé à l'identifiant de résultat
    <parameter>result_id</parameter>. La ligne est retournée sous la
    forme d'un tableau associatif.
   </para>
   <para>
    Les appels ultérieurs à <function>ifx_fetch_row</function>
    retourneront la ligne suivante, ou &false; s'il n'y a plus de ligne.
   </para>
   <example>
    <title>Exemple avec <function>ifx_fetch_row</function></title>
    <programlisting role="php">
<![CDATA[
<?php
$rid = ifx_prepare ("select * from emp where name like " . $name,
                     $connid, IFX_SCROLL);
if (!$rid) {
//    ... erreur ...
}
$rowcount = ifx_affected_rows($rid);
if ($rowcount > 1000) {
    printf ("Trop de lignes dans le résultat. (%d)\n<BR>", $rowcount);
    die ("Recommencez votre requête. <BR>\n");
}
if (! ifx_do ($rid)) {
//   ... erreur ...
}
$row = ifx_fetch_row ($rid, "NEXT");
while (is_array($row)) {
    for(reset($row); $fieldname=key($row); next($row)) {
        $fieldvalue = $row[$fieldname];
        printf ("%s = %s,", $fieldname, $fieldvalue);
    }
    printf("\n<BR>");
    $row = ifx_fetch_row ($rid, "NEXT");
}
ifx_free_result ($rid);
?>
]]>
    </programlisting>
   </example>
  </refsect1>
 </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
