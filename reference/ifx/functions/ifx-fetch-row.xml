<?xml version="1.0" encoding="iso-8859-1"?>
<!-- splitted from ./fr/functions/ifx.xml, last change in rev 1.9 -->
<!-- last change to 'ifx-fetch-row' in en/ tree in rev 1.2 -->
 <refentry id="function.ifx-fetch-row">
  <refnamediv>
   <refname>ifx_fetch_row</refname>
   <refpurpose>
    Retourne une ligne sous la forme d'un tableau &eacute;num&eacute;r&eacute;.
   </refpurpose>
  </refnamediv>
  <refsect1>
   <title>Description</title>
    <methodsynopsis>
     <type>array</type><methodname>ifx_fetch_row</methodname>
     <methodparam><type>int</type><parameter>result_id</parameter></methodparam>
     <methodparam choice="opt"><type>mixed</type><parameter>position</parameter></methodparam>
    </methodsynopsis>
   <para>
    <function>ifx_fetch_row</function> retourne un tableau associatif qui
    contient la ligne retourn&eacute;e, ou &false; s'il ne reste plus de
    lignes &agrave; lire, ou s'il a eu une erreur.
   </para>
   <para>
    Les colonnes de types BLOB sont retourn&eacute;es sous la forme d'un
    identifiant &agrave; utiliser avec <function>ifx_get_blob</function>
    &agrave; moins que vous n'ayez utilis&eacute; la fonction
    <function>ifx_textasvarchar</function> ou
    <function>ifx_byteasvarchar</function>, et dans ce cas, les BLOBs
    seront retourn&eacute;s sous forme de cha&icirc;ne.
    <function>ifx_fetch_row</function> retourne &false;
    en cas d'erreur.
   </para>
   <para>
    <parameter>result_id</parameter> est un identifiant valide de r&eacute;sultat,
    retourn&eacute; par <function>ifx_query</function> ou
    <function>ifx_prepare</function> (Requ&ecirc;tes SELECT seulement !).
   </para>
   <para>
    <parameter>position</parameter> est un param&egrave;tre
    optionnel, pour une op&eacute;ration de lecture d'informations sur un pointeur
    de type "scroll": "NEXT", "PREVIOUS", "CURRENT", "FIRST", "LAST" ou encore un
    nombre. Si vous sp&eacute;cifiez un nombre, la ligne d'index absolu sera
    retourn&eacute;e. Ce param&egrave;tre est optionnel, et ne fonctionne qu'avec
    les pointeurs de type "scroll".
   </para>
   <para>
    <function>ifx_fetch_row</function> retourne une ligne de donn&eacute;es d'un
    r&eacute;sultat associ&eacute; &agrave; l'identifiant de r&eacute;sultat
    <parameter>result_id</parameter>. La ligne est retourn&eacute;e sous la
    forme d'un tableau associatif.
   </para>
   <para>
    Les appels ult&eacute;rieurs &agrave; <function>ifx_fetch_row</function>
    retourneront la ligne suivante, ou &false; s'il n'y a plus de ligne.
   </para>
   <example>
    <title>Exemple avec <function>ifx_fetch_row</function></title>
    <programlisting role="php">
&lt;?php
$rid = ifx_prepare ("select * from emp where name like " . $name,
                     $connid, IFX_SCROLL);
if (! $rid) {
//    ... erreur ...
}
$rowcount = ifx_affected_rows($rid);
if ($rowcount &gt; 1000) {
    printf ("Trop de lignes dans le r&eacute;sultats. (%d)\n&lt;br&gt;", $rowcount);
    die ("Recommencez votre requ&ecirc;te. &lt;br&gt;\n");
}
if (! ifx_do ($rid)) {
//   ... erreur ...
}
$row = ifx_fetch_row ($rid, "NEXT");
while (is_array($row)) {
    for(reset($row); $fieldname=key($row); next($row)) {
        $fieldvalue = $row[$fieldname];
        printf ("%s = %s,", $fieldname, $fieldvalue);
    }
    printf("\n&lt;br&gt;");
    $row = ifx_fetch_row ($rid, "NEXT");
}
ifx_free_result ($rid);
?&gt;
    </programlisting>
   </example>
  </refsect1>
 </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
