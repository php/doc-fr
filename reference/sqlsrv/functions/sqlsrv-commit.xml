<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
<!-- EN-Revision: c758e862cf7648f657acb9e073bcc657f368a701 Maintainer: yannick Status: ready -->
<!-- Reviewed: no -->
<refentry xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="function.sqlsrv-commit">
 <refnamediv>
  <refname>sqlsrv_commit</refname>
  <refpurpose>Valide une transaction commencée avec la fonction <function>sqlsrv_begin_transaction</function></refpurpose>
 </refnamediv>
 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <type>bool</type><methodname>sqlsrv_commit</methodname>
   <methodparam><type>resource</type><parameter>conn</parameter></methodparam>
  </methodsynopsis>
  <simpara>
   Valide une transaction commencée par la fonction <function>sqlsrv_begin_transaction</function>.
   La connexion est retournée en mode auto-commit après l'appel à la fonction
   <function>sqlsrv_commit</function>. La transaction validée inclut toutes les
   requêtes exécutées après un appel à la fonction
   <function>sqlsrv_begin_transaction</function>. Les transactions doivent commencer,
   être valider ou être annuler en utilisant ces fonctions au lieu d'utiliser des requêtes
   déclarant le début ou l'annulation d'une transaction. Pour plus d'informations,
   reportez-vous aux <link xlink:href="&url.sqlsrv.transaction.handling;">transactions SQLSRV</link>.
  </simpara>
 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  <variablelist>
   <varlistentry>
    <term><parameter>conn</parameter></term>
    <listitem>
     <simpara>
      La connexion sur laquelle la transaction va être validée.
     </simpara>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <simpara>
   &return.success;
  </simpara>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <example>
   <title>Exemple avec <function>sqlsrv_commit</function></title>
   <simpara>
    L'exemple suivant montre la façon d'utiliser la fonction
    <function>sqlsrv_commit</function> avec
    <function>sqlsrv_begin_transaction</function> et
   <function>sqlsrv_rollback</function>.
   </simpara>
   <programlisting role="php">
<![CDATA[
<?php
$serverName = "serverName\sqlexpress";
$connectionInfo = array( "Database"=>"dbName", "UID"=>"userName", "PWD"=>"password");
$conn = sqlsrv_connect( $serverName, $connectionInfo);
if( $conn === false ) {
    die( print_r( sqlsrv_errors(), true ));
}

/* Début de la transaction. */
if ( sqlsrv_begin_transaction( $conn ) === false ) {
     die( print_r( sqlsrv_errors(), true ));
}

/* Initialisation des paramètres. */
$orderId = 1; $qty = 10; $productId = 100;

/* Définition et exécution de la première requête. */
$sql1 = "INSERT INTO OrdersTable (ID, Quantity, ProductID)
         VALUES (?, ?, ?)";
$params1 = array( $orderId, $qty, $productId );
$stmt1 = sqlsrv_query( $conn, $sql1, $params1 );

/* Définition et exécution de la seconde requête. */
$sql2 = "UPDATE InventoryTable
         SET Quantity = (Quantity - ?)
         WHERE ProductID = ?";
$params2 = array($qty, $productId);
$stmt2 = sqlsrv_query( $conn, $sql2, $params2 );

/* Si les deux requêtes ont réussi, on commit la transaction. */
/* Sinon, on l'annule. */
if( $stmt1 && $stmt2 ) {
     sqlsrv_commit( $conn );
     echo "Transaction validée.<br />";
} else {
     sqlsrv_rollback( $conn );
     echo "Transaction annulée.<br />";
}
?>
]]>
   </programlisting>
  </example>
 </refsect1>

 <refsect1 role="seealso">
  &reftitle.seealso;
  <simplelist>
   <member><function>sqlsrv_begin_transaction</function></member>
   <member><function>sqlsrv_rollback</function></member>
  </simplelist>
 </refsect1>
</refentry>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
