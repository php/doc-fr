<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.3 $ -->
<!-- EN-Revision: 1.3 Maintainer: yannick Status: ready -->
<refentry id="function.oci-new-connect">
   <refnamediv>
    <refname>oci_new_connect</refname>
    <refpurpose>Etablit une nouvelle connexion à Oracle</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Description</title>
     <methodsynopsis>
      <type>resource</type><methodname>oci_new_connect</methodname>
      <methodparam><type>string</type><parameter>username</parameter></methodparam>
      <methodparam><type>string</type><parameter>password</parameter></methodparam>
      <methodparam choice="opt"><type>string</type><parameter>db</parameter></methodparam>
     </methodsynopsis>
    <para>
     <function>oci_new_connect</function> crée une nouvelle connexion à un
     serveur Oracle, et s'identifie avec le nom d'utilisateur <parameter>username</parameter>
     et le mot de passe <parameter>password</parameter>. Le troisième 
     paramètre, optionnel, <parameter>db</parameter> peut contenir l'instance
     locale d'Oracle ou un nom disponible dans le fichier 
     <filename>tnsnames.ora</filename>. Si le troisième paramètre 
     n'est pas fourni, &php; va chercher la valeur dans les variables
     d'environnement <literal>ORACLE_SID</literal> et 
     <literal>TWO_TASK</literal> pour déterminer le nom de l'instance
     locale d'Oracle, et la localisation du fichier 
     <filename>tnsnames.ora</filename>.
    </para>
    <para>
     <function>oci_new_connect</function> force la création d'une nouvelle connexion.
     Cette fonction doit être utilisée si vous souhaitez isoler des
     transactions. Par défaut, les connexions sont partagées, et 
     des appels successifs à <function>oci_connect</function> vous retourneront
     la même connexion.
    </para>
    <para>
     Voici comment séparer des transactions.
     <example>
      <title>Exemple avec <function>oci_new_connect</function></title>
      <programlisting role="php">
<![CDATA[
<?php
echo "<html><pre>";
$db = "";

$c1 = oci_connect("scott", "tiger", $db);
$c2 = oci_new_connect("scott", "tiger", $db);

function create_table($conn) 
{
  $stmt = oci_parse($conn, "create table scott.hallo (test
varchar2(64))");
  oci_execute($stmt);
  echo $conn . " created table\n\n";
}

function drop_table($conn) 
{
  $stmt = oci_parse($conn, "drop table scott.hallo");
  oci_execute($stmt);
  echo $conn . " dropped table\n\n";
}

function insert_data($conn) 
{
  $stmt = oci_parse($conn, "insert into scott.hallo 
            values('$conn' || ' ' || to_char(sysdate,'DD-MON-YY HH24:MI:SS'))");
  oci_execute($stmt, OCI_DEFAULT);
  echo $conn . " inserted hallo\n\n";
}

function delete_data($conn) 
{
  $stmt = oci_parse($conn, "delete from scott.hallo");
  oci_execute($stmt, OCI_DEFAULT);
  echo $conn . " deleted hallo\n\n";
}

function commit($conn) 
{
  oci_commit($conn);
  echo $conn . " committed\n\n";
}

function rollback($conn) 
{
  oci_rollback($conn);
  echo $conn . " rollback\n\n";
}

function select_data($conn) 
{
  $stmt = oci_parse($conn, "select * from scott.hallo");
  oci_execute($stmt, OCI_DEFAULT);
  echo $conn . "----selecting\n\n";
  while (oci_fetch($stmt)) {
    echo $conn . " <" . oci_result($stmt, "TEST") . ">\n\n";
  }
  echo $conn . "----done\n\n";
}

create_table($c1);
insert_data($c1);

select_data($c1);   
select_data($c2);   

rollback($c1);      

select_data($c1);   
select_data($c2);   

insert_data($c2);   
commit($c2);        

select_data($c1);   

delete_data($c1);   
select_data($c1);   
select_data($c2);   
commit($c1);        

select_data($c1);
select_data($c2);

drop_table($c1);
echo "</pre></html>";
?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     <function>oci_new_connect</function> retourne &false; en cas d'erreur.
    </para>
    <note>
     <para>
      Dans les versions de &php; antérieures à la version 5.0.0,
      vous devez utiliser la fonction <function>ocinlogon</function>. 
      Cet ancien nom est toujours utilisable : un alias a été fait vers
      la fonction <function>oci_new_connect</function>, pour assurer
      la compatibilité ascendante. Toutefois, il est recommandé de ne
      plus l'utiliser.
     </para>
    </note>
     <simpara>
     Voir aussi
     <function>oci_connect</function> et
     <function>oci_pconnect</function>.
    </simpara>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
