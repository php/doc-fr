<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.3 $ -->
<!-- EN-Revision: 1.3 Maintainer: yannick Status: ready -->
  <refentry id="function.oci-connect">
   <refnamediv>
    <refname>oci_connect</refname>
    <refpurpose>Etablit une connexion avec un serveur Oracle</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Description</title>
     <methodsynopsis>
      <type>resource</type><methodname>oci_connect</methodname>
      <methodparam><type>string</type><parameter>username</parameter></methodparam>
      <methodparam><type>string</type><parameter>password</parameter></methodparam>
      <methodparam choice="opt"><type>string</type><parameter>db</parameter></methodparam>
     </methodsynopsis>
    <para>
     <function>oci_connect</function> retourne une ressource de
     connexion, nécessaire à la plupart des appels OCI. Le troisième
     paramètre, optionnel, contient le nom de l'instance locale d'Oracle,
     ou le nom de l'entrée dans le fichier <filename>tnsnames.ora</filename> 
     auquel vous voulez vous connecter. Si le troisième paramètre optionnel
     n'est pas spécifié, &php; va utiliser la variable d'environnement
     <constant>ORACLE_SID</constant> (Oracle instance) ou
     <constant>TWO_TASK</constant> (<filename>tnsnames.ora</filename>) 
     pour déterminer la base à laquelle se connecter.
    </para>
    <note>
     <simpara>
      <function>oci_connect</function> <emphasis>ne crée pas</emphasis> de
      nouvelle connexion si une connexion avec les mêmes paramètres a 
      été établie auparavant. Dans ce cas, <function>oci_connect</function> 
      va retourner la ressource déjà générée. Cela signifie que vous ne pouvez
      pas utiliser cette fonction pour mener 2 transactions paralelles. 
      Pour établir une connexion distincte, vous devez utiliser la fonction
      <function>oci_new_connect</function>.
     </simpara>
    </note>
    <para>
     <example>
      <title>Exemple avec <function>oci_connect</function></title>
      <programlisting role="php">
<![CDATA[
<?php
echo "<pre>";
$db = "";

$c1 = oci_connect("scott", "tiger", $db);
$c2 = oci_connect("scott", "tiger", $db);

function create_table($conn) 
{
  $stmt = oci_parse($conn, "create table scott.hallo (test varchar2(64))");
  oci_execute($stmt);
  echo $conn . " created table\n\n";
}

function drop_table($conn) 
{
  $stmt = oci_parse($conn, "drop table scott.hallo");
  oci_execute($stmt);
  echo $conn . " dropped table\n\n";
}

function insert_data($conn) 
{
  $stmt = oci_parse($conn, "insert into scott.hallo 
            values('$conn' || ' ' || to_char(sysdate,'DD-MON-YY HH24:MI:SS'))");
  oci_execute($stmt, OCI_DEFAULT);
  echo $conn . " inserted hallo\n\n";
}

function delete_data($conn) 
{
  $stmt = oci_parse($conn, "delete from scott.hallo");
  oci_execute($stmt, OCI_DEFAULT);
  echo $conn . " deleted hallo\n\n";
}

function commit($conn) 
{
  oci_commit($conn);
  echo $conn . " committed\n\n";
}

function rollback($conn) 
{
  oci_rollback($conn);
  echo $conn . " rollback\n\n";
}

function select_data($conn) 
{
  $stmt = oci_parse($conn, "select * from scott.hallo");
  oci_execute($stmt, OCI_DEFAULT);
  echo $conn."----selecting\n\n";
  while (oci_fetch($stmt)) {
    echo $conn . " [" . oci_result($stmt, "TEST") . "]\n\n";
  }
  echo $conn . "----done\n\n";
}

create_table($c1);
insert_data($c1);   // Insertion d'une ligne via c1
insert_data($c2);   // Insertion d'une ligne via c2

select_data($c1);   // Résultat des insertions
select_data($c2);   

rollback($c1);      // Annulation sur c1

select_data($c1);   // Les deux insertions ont été annulées
select_data($c2);   

insert_data($c2);   // Insertion d'une ligne via c2
commit($c2);        // Validation via c2

select_data($c1);   // Le résultat de c2 est retourné

delete_data($c1);   // Effacement de toute la table via c1
select_data($c1);   // Aucune ligne de trouvée
select_data($c2);   // Aucune ligne de trouvée
commit($c1);        // Validation via c1

select_data($c1);   // Aucune ligne de trouvée
select_data($c2);   // Aucune ligne de trouvée

drop_table($c1);
echo "</pre>";
?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     <function>oci_connect</function> retourne &false; si une erreur survient.
    </para>
    <note>
     <para>
      Dans les versions de &php; antérieures à la version 5.0.0,
      vous devez utiliser la fonction <function>ocilogon</function>. 
      Cet ancien nom est toujours utilisable : un alias a été fait vers
      la fonction <function>oci_connect</function>, pour assurer
      la compatibilité ascendante. Toutefois, il est recommandé de ne
      plus l'utiliser.
     </para>
    </note>    
    <simpara>
     Voir aussi
     <function>oci_pconnect</function> et
     <function>oci_new_connect</function>.
    </simpara>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
