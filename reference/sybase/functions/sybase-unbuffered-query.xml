<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Revision: 1.2 $ -->
  <refentry id="function.sybase-unbuffered-query">
   <refnamediv>
    <refname>sybase_unbuffered_query</refname>
    <refpurpose>Envoie une requ&ecirc;te &agrave; Sybase et ne bloque pas</refpurpose>
   </refnamediv>
   <refsect1>
    <title>Description</title>
     <methodsynopsis>
      <type>resource</type><methodname>sybase_unbuffered_query</methodname>
      <methodparam><type>string</type><parameter>query</parameter></methodparam>
      <methodparam><type>resource</type><parameter>link_identifier</parameter></methodparam>
     </methodsynopsis>
    <para> 
    <function>sybase_unbuffered_query</function> retourne une ressource
    Sybase result en cas de r&eacute;ussite, et &false; sinon.
    </para>
    <para> 
     <function>sybase_unbuffered_query</function> envoie la requete <parameter>query</parameter>
     au serveur Sybase actif, repr&eacute;sent&eacute; par <parameter>link_identifier</parameter>.
     Si <parameter>link_identifier</parameter> n'est pas sp&eacute;cifi&eacute;, la derni&egrave;re
     connexion ouverte sera utilis&eacute;e. Si aucune connexion n'a &eacute;t&eacute; ouverte,
     la fonction essaiera d'&eacute;tablir un lien avec <function>sybase_connect</function>,
     et de l'utiliser.
    </para>
    <para>
     Contrairement &agrave; <function>sybase_query</function>,
     <function>sybase_unbuffered_query</function> lit uniquement la premi&egrave;re
     ligne lue. <function>sybase_fetch_array</function> et les fonctions
     similaires lisent les lignes n&eacute;cessaires. <function>sybase_data_seek</function>
     lit jusqu'&agrave; la prochaine ligne. Ce comportement fournit des performances
     accrues pour les r&eacute;sultats de grande taille.
    </para>
    <para>
     <function>sybase_num_rows</function> ne retournera le nombre de lignes
     correct que si toutes les lignes du r&eacute;sultat ont &eacute;t&eacute; lues. Pour
     Sybase, le nombre de ligne n'est pas connu, et doit &ecirc;tre calcul&eacute; par 
     le programme client.
    </para>
    <note>
     <para>
      Si vous ne lisez pas toutes les lignes du r&eacute;sultat avant d'ex&eacute;cuter une
      nouvelle requ&ecirc;te, &php; va g&eacute;n&eacute;rer une alerte, et annuler toutes les lignes
      qui restent &agrave; lire. Pour &eacute;viter cela, utilise la fonction 
      <function>sybase_free_result</function> qui va annuler les lignes restantes.
     </para>
    </note>
    <para>
     Le param&egrave;tre optionnel <parameter>store_result</parameter> peut valoir &false;
     pour indiquer que les lignes ne doivent pas &ecirc;tre ramen&eacute; en m&eacute;moire, afin de
     r&eacute;duire la consommation, surtout dans le cas de tr&egrave;s grands r&eacute;sultats.
    </para>
    <example>
     <title>Exemple avec <function>sybase_unbuffered_query</function></title>
     <programlisting role="php">
<![CDATA[
<?php
       $dbh= sybase_connect('SYBASE', '', '');
       $q= sybase_unbuffered_query('select firstname, lastname from huge_table', $dbh, FALSE);
       sybase_data_seek($q, 10000);
       $i= 0;
       while ($row= sybase_fetch_row($q)) {
               echo $row[0].' '.$row[0];
               if ($i++ > 40000) break;
       }
       sybase_free_result($q);
       sybase_close($dbh);
?>
]]>
     </programlisting>
    </example>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
